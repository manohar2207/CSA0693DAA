#include<stdio.h>
long factorial(int n)
{
	if(n==0 || n==1)
	return 1;
	else 
	return (n*factorial(n-1));
}
int main()
{
	int number;
	long fact;
	printf("enter the number:/n");
	scanf("%d",&number);
	fact=factorial(number);
	printf("the factorial of %d is %d:/n",number,fact);
	return 0;
}
